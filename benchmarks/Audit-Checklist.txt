PHASE 7 — Security & Hardening Audit (v0.9.3)
==============================================

PATH TRAVERSAL AUDIT
  Fixed: hashing.ts assertSafePattern() — previous check (`startsWith("..")`)
  did not catch `./../../etc/passwd`-style patterns. Now normalises separators
  and rejects any ".." segment in the split path. All other path.join() calls
  operate on config-controlled input (developer-authored antiscale.config.ts),
  which is in the trust boundary.

COMMAND INJECTION AUDIT
  executor.ts: execa() is called with an explicit args array (via string-argv),
  never via shell. No shell interpolation of user-supplied strings. Safe.
  git-diff.ts: execa("git", ["diff","--name-only", baseRef]) — array args, safe.

INPUT VALIDATION COMPLETENESS
  All config keys flow through antiscaleConfigSchema (Zod). Types, defaults,
  and cross-field invariants (dependsOn references) are enforced. No raw
  config values reach the filesystem or shell without schema processing.

CACHE KEY INTEGRITY
  SHA-256 over sorted (path, content) pairs. An attacker controlling file
  content cannot produce a chosen hash without a SHA-256 pre-image attack.
  No meaningful collision risk.

DEPENDENCY AUDIT
  Production deps: commander, execa, fast-glob, jiti, picocolors, string-argv,
  ts-morph, zod. Run `pnpm audit` before each release; resolve any high/
  critical findings before shipping.

BENCHMARK (startup)
PROMPT: Measure-Command { node dist/index.js --help } PowerShell

# Before phase-8 fixes (tsup/tsx/@types/node in dependencies, stale dist):
Milliseconds      : 706
TotalMilliseconds : 706.2186

# After phase-8 fixes (lazy imports, dev tooling moved to devDependencies, fresh build):
Milliseconds      : 172
TotalMilliseconds : 172.962

# After rename to `antiscaler` (fresh build, identical architecture):
TotalMilliseconds : 193.938

Baseline for 0.1.0 release: ~170-195ms (cold-cache jitter of ~20ms is normal).
Plan's original aspirational target was <100ms; current numbers are well
under the "feels instant" threshold (~200ms) for a CLI and are accepted
as the shipping baseline. Revisit if startup regresses above 250ms.

tsup output confirms the lazy-import architecture:
  dist/index.js  1.89 KB   (entry — only commander + error class)
  dist/build-*.js   907 B  (one chunk per command, loaded on demand)
  dist/dev-*.js     587 B
  dist/run-*.js     907 B
  dist/init-*.js    664 B
  dist/env-*.js     515 B
  dist/insight-*.js 682 B
  dist/check-*.js   861 B
Heavy deps (execa, jiti, fast-glob) are pulled in only by the command
chunks that actually need them.
